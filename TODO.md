#

&#9745; Продумать как будет ораганизована инициализация режимов работы микросервиса.

&#9745; Разобраться с адаптерами и портами и как их интегрировать в чистую архитектуру.

    - «Снаружи → (входной адаптер) → входной порт → ЮЗКЕЙС → выходной порт → (выходной адаптер) → вовне»
    - client → gRPC handler → usecase.AddTask → repo.Save → Postgres
        (входн. адаптер)  (входн. порт)  (выходн. порт) (выходн. адаптер)

&#9744; Написать юзкейс по выкачке данных.

    + найти ассет провайдер
    + реализовать репозиотрий
    + реализвоать txmanager
    + реализовать outbox writer с txmanager
    + продумать дто, энтити и схему БД для outbox
    + продумать дто, энтити и схему БД для ассетов
    - реализвоать nats паблишер

&#9744; Поднять инфраструктуру БД и НАТС.

    + БД
    - НАТС

&#9744; Добавить воркер мод с хелс сервером.

    - реализовать outbox worker

&#9744; Написать модуль конфигов

&#9744; Добавить натс потребителя в воркер мод который читает с сабжекта ивенты IngestTokenList, IngestChainMetadata.

&#9744; Добавить постгрес в воркер мод.

&#9744; Добавить планировщик мод с хелс сервером.

&#9744; Запускать данный юзкейс по расписанию и отправлять в брокер, а после воркером читать из брокреа и выполнять выкачку.
